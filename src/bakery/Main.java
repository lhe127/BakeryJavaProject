package bakery;

/**
 * The entry point for the bakery application.
 * This class contains the main method which initializes the bakery system.
 */
public class Main {

    /**
     * The main method that starts the bakery application.
     * This method initializes the bakery system by creating an instance of the Bakery class.
     *
     * @param args Command-line arguments (not used in this case).
     */
    public static void main(String[] args) {
        new Bakery();
    }
}